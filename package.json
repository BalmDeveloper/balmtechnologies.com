{
  "name": "balmtechnologies-com",
  "private": true,
  "version": "1.0.0",
  "description": "Balm Technologies marketing site",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
